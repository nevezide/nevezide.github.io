/********** Exercice accumulateur **********/
// L'accumulateur est un compteur qui s'incrémente ou se décrémente.

// On demande à l'utilisateur de saisir une valeur ou une commande (nothing / stop / + / -)
// Si l'utilisateur saisi + ou -, on change l'opérateur courant en "+" ou "-"
// Si la saisie est un nombre : on ajoute ou soustrait la valeur à l'accumulateur selon l'opérateur courant
// Si la saisie est la commande "stop", on arrête la boucle
// Si la saisie est la commande "nothing", on ne fait rien (on continue la boucle)
//                                         jusqu'à ce que l'utilisateur saisisse une autre commande
// On affiche la valeur de l'accumulateur à chaque itération de la boucle

// !!! Eviter de répéter le code, préférer l'utilisation de continue / break !!!
